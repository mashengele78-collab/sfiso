# Copy to .env and fill in. Never commit real tokens.
FB_ACCESS_TOKEN=replace_me
FB_APP_ID=replace_me
FB_APP_SECRET=replace_me
# Optional
# FB_API_VERSION=v26.0
# FB_PROFILE=default
# Point the CLI at a local mock Graph server (testing only)
# FB_GRAPH_HOST=http://127.0.0.1:8899
